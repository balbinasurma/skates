import React from 'react'
import styled from 'styled-components'
import {styledMainType} from '../types/styledTypes'

const AppMain: styledMainType = styled.main``

const App = () => <AppMain>app</AppMain>

export default App
